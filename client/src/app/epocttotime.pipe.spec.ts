import { EpocttotimePipe } from './epocttotime.pipe';

describe('EpocttotimePipe', () => {
  it('create an instance', () => {
    const pipe = new EpocttotimePipe();
    expect(pipe).toBeTruthy();
  });
});
